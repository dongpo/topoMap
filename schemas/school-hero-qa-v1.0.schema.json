{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.nma.local/nma.school-hero-qa/1.0",
  "title": "NMA School Hero Deterministic QA 1.0",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema", "qa_id", "execution_id", "status", "classification", "expected_transition", "observed_transition", "checks", "qa_sha256"],
  "properties": {
    "schema": {"const": "nma.school-hero-qa/1.0"},
    "qa_id": {"type": "string", "pattern": "^qa-[0-9a-f]{24}$"},
    "execution_id": {"type": "string", "pattern": "^exec-[0-9a-f]{24}$"},
    "status": {"enum": ["passed", "failed"]},
    "classification": {"enum": ["expected-change-verified", "expected-change-missing", "incorrect-change", "unexpected-additional-change"]},
    "expected_transition": {"type": "object"},
    "observed_transition": {"type": "object"},
    "checks": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "status", "expected", "observed"],
        "properties": {
          "id": {"type": "string", "minLength": 1},
          "status": {"enum": ["passed", "failed"]},
          "expected": {},
          "observed": {}
        }
      }
    },
    "qa_sha256": {"type": "string", "pattern": "^[0-9a-f]{64}$"}
  }
}
