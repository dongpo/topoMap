# BUILD-09E1 Completion Report

## Verdict

**PASS — PORTRAYAL EVIDENCE CLOSED; J13/J17 PRODUCTION BINDING STILL MISSING**

BUILD-09E1 closes the official authority boundaries for portrayal line code `2` and colour code
`7`. The available versioned authoritative material defines the logical `BUILD` polygon product
schema but does not publish the final package-member-to-production applicability arrow needed to
select `J13_BUILD` or `J17_BUILD`. No production implementation or activation is authorized.

BUILD-10 readiness is therefore exactly:

`J13-J17-BINDING-STILL-BLOCKING`

## 1. Starting gate

- canonical repository: `https://github.com/dongpo/topoMap.git`;
- canonical root: the repository containing this report;
- clean starting branch: `build/build-09e-official-evidence-closure`;
- exact BUILD-09E predecessor commit: `e46ea5eb10f6a177ab084d6ca8743c1011f4c1fd`;
- BUILD-09E evidence closure: `bfee262f17b5bc99ff8e55f6b284917cf5507aaa80b0e3bae2454e35da4fbaed`;
- BUILD-09 contract: `0b9e0cc9c98274f9efcbed451905fa21857c33f0ec9472254fa6e3b803c24a0c`;
- BUILD-08A authorization: `4eedc443d4f1d5c0af36e696fc67fd0101f6936d78edba19d5c20d41ab2b8da8`;
- predecessor readiness: `OFFICIAL-EVIDENCE-STILL-MISSING`;
- predecessor gaps: exactly J13/J17 production binding, line-code-2 physical width, and
  colour-code-7 device value or authorized conversion;
- unresolved authoritative-to-authoritative conflicts: none.

The required BUILD-09E1 branch is:

`build/build-09e1-targeted-official-binding-portrayal-resolution`

## 2. Frozen BUILD-09E findings

The record copies and validates, without reinterpretation:

- annotation content: `officially-supported`, floor count followed by structure;
- annotation placement: `local-policy-required` within official center/near-feature and avoidance
  semantics;
- hatch spacing: `2 mm`, `officially-supported`;
- hatch angle: `local-policy-required-with-official-diagonal-semantics`, with no numeric angle;
- hatch resource: `local-policy-required`, with no asset created or deployed;
- PolygonZ/derived XY: unchanged `P2-production-candidate` architecture, immutable and recoverable
  source Z, non-authoritative/non-writing derived XY, destructive drop-z incompatible;
- production activation, official portrayal activation, source mutation, geometry repair, Z removal,
  and derived-output writeback: forbidden.

The five BUILD-09E predecessor files are bound by file SHA-256 in the BUILD-09E1 record and remain
unchanged.

## 3. J13/J17 production binding

Resolution:

`official-binding-not-published-or-not-available`

Available authoritative trace:

1. official specification: `多維度繪製圖層V4.xlsx`;
2. specification version: V4, decision date 2023-11-10;
3. dataset/product/package: multidimensional spatial-information base-map layer definition;
4. geographic/product scope: logical product schema, with no J13/J17 member scope assignment;
5. layer code: `BUILD`;
6. layer title/meaning: `(三)建物BUILD(面)`;
7. geometry: polygon in the V4 definition; the observed package members are PolygonZ;
8. field schema: `BUILD_ID,TERRAINID,BUILD_STR,BUILD_NO,BUILD_H,GROUP_ID,MDATE`;
9. Building semantic role: permanent, under-construction, and temporary Building polygons;
10. NMA production applicability: not published or available for `J13_BUILD` versus `J17_BUILD`.

The V4 workbook is identified as Drive file `1C12PmP-8ZZtZbHVKRAKv0mmWE22kgzEw`, size 569257,
modified `2024-01-25T08:59:54.631Z`, raw SHA-256
`d3f065a57d10c306e9e4e686641c07ddf2fb5a6d3638b68ec4d3ea533839f308`.

The immutable source package establishes only that `J13_BUILD` is the Baoshan urban-plan member and
`J17_BUILD` is the Hsinchu Science Park special-plan (Baoshan portion) member. It does not publish
which member is applicable to the intended NMA production contract. J13 DEMO use and J17 runtime
history remain non-promoting implementation evidence.

Deterministic selection criterion:

`selection-forbidden-until-an-authoritative-versioned-package-manifest-binds-J13_BUILD-or-J17_BUILD-to-the-NMA-production-contract`

No private source archive was accessed or inferred. Search absence is not treated as proof that no
historical rule existed.

## 4. Line code 2

Resolution:

`official-physical-width-established`

The NLSC-commissioned `多維度圖式研究案報告(修訂版).pdf`, Figure 2-3, defines:

- code: `2` / `2號線`;
- original physical specification: `0.20`;
- original unit: `mm` (`公釐`).

The source is Drive file `1C-2N1JDxr9JHc6s2b2AvHJ1HwxndCXRy`, project `NLSC-112-18`,
commissioning agency `內政部國土測繪中心`, report date `112-11-20`, Drive size 12003802,
modified `2024-01-25T08:59:54.631Z`, raw SHA-256
`0f8931be648077d6775d1c866376fef775bb3f8568448f94d30493d56b945fda`, printed page 10 / PDF
page 22.

No CSS-pixel value is stated. In particular, `1 CSS px` is not authorized.

## 5. Colour code 7

Resolution:

`official-device-value-established`

The same Figure 2-3 defines:

- code: `7`;
- semantic name: `黑色` (black);
- original representation heading: `RGB值 (R-G-B)`;
- original value: `(0,0,0)`.

The record preserves this notation exactly. It does not relabel `#000000`, `#111111`, or any other
HEX/CSS notation as official because the cited table states RGB, not HEX.

## 6. Output-profile boundary

The official/local boundary is closed as:

`official portrayal semantics → authorized local output profile → MapLibre/device representation`

Status:

`local-output-profile-policy-required`

The later human profile must preserve official `0.20 mm` and RGB `(0,0,0)` while deciding physical
millimetres to CSS pixels, screen scale/DPI, MapLibre colour serialization, opacity, and antialiasing.
BUILD-09E1 chooses none of those local values.

## 7. Five Building gates

1. hatch angle/asset: `local-policy-required`;
2. annotation placement/binding: `local-policy-required`;
3. J13/J17 schema identity: `HOLD-indeterminate`;
4. line/colour portrayal: `local-output-profile-policy-required`;
5. PolygonZ/derived XY: `P2-production-candidate`.

## 8. Deterministic artifacts

- evidence-resolution record:
  `data/specifications/nma-build-09e1-golden-targeted-official-evidence-resolution-v1.0.json`;
- canonical record SHA-256:
  `f75c44bcb834090277588b3c23cfe48f00e965c947754497f64831d4b47b9b65`;
- successor candidate:
  `data/specifications/nma-build-09e1-successor-building-production-contract-candidate-v1.0.json`;
- successor contract SHA-256:
  `e1bf4c251ae69739f3455c015ea7bac6b9de98742f3ebc2195da75adb2fc6cba`;
- successor status: `evidence-hold`.

The successor binds the exact BUILD-09 contract, BUILD-09E closure, and BUILD-09E1 record. It
preserves all official semantics, PolygonZ P2, local-policy boundaries, activation prohibitions, and
source-mutation prohibitions.

## 9. Verification

- focused BUILD-09E1: **38 passed**;
- BUILD-00A through BUILD-09E1 historical regression: **525 passed; 3 failed**;
- frozen Core integrity: **53 passed**;
- frozen ROAD integrity: **199 passed**;
- frozen School Hero integrity: **23 passed**;
- complete repository regression with no deselections: **1124 passed; 6 failed**.

The three BUILD-chain failures are inherited stage-local descendant-scope assertions:

1. `tests/test_build_human_official_production_scope_authorization_build08a.py::test_previous_build_and_forbidden_artifacts_remain_unchanged`;
2. `tests/test_building_production_contract_build09.py::test_previous_build_frozen_artifacts_and_runtime_remain_unchanged`;
3. `tests/test_official_evidence_closure_build09e.py::test_previous_artifacts_runtime_and_source_scope_remain_unchanged`.

Each fails only because its frozen allowed-file set intentionally excludes later BUILD stage files.
The reported extras are the authorized BUILD-09/09E descendants or the seven BUILD-09E1 files; no
predecessor or runtime file changed.

The complete suite additionally reproduces the three known Agentic/demo drift failures:

1. `tests/test_agentic_demo_catalog.py::test_pmtiles_capability_catalog_is_reproducible`;
2. `tests/test_agentic_freeze.py::test_agentic_v03_freeze_verifies_current_and_historical_boundaries`;
3. `tests/test_agentic_v03_pages.py::test_agentic_v03_pages_candidate_preserves_v02_and_passes_every_gate`.

These concern pre-existing PMTiles catalog and Agentic freeze/source-asset drift. They were classified
and not repaired, as required. No other test failed.

## 10. Scope

BUILD-09E1 adds only its non-executing builder/validator, two deterministic JSON records, two JSON
schemas, focused tests, and this report. It does not change `src/`, production runtime behavior,
source archives, source geometry, portrayal assets, predecessor BUILD artifacts, Core, ROAD, or
School Hero files.

## 11. Next stage

Do not begin BUILD-09F or BUILD-10. Recommend another targeted continuation only for the exact
versioned J13/J17 package-member production applicability gap. Do not reopen portrayal evidence.
